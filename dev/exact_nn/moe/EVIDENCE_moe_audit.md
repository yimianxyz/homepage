# Phase-2 MoE — 5-lens adversarial audit synthesis (side-a)

I have enough. The label `bicls` is the coord-dedup class (lowest-index member), and S_dec is measured at coord-class level — matching SPEC §3's explicit coord-dedup definition. So the spec-compliance lens's "coordinate-deduping may hide decision-level variance" is a nit that the SPEC itself prescribes; not a flaw.

I now have full ground truth. Let me write the synthesis.

---

# LEAD AUDITOR SYNTHESIS — Phase-2 MoE S_dec result (side-a)

## 1. OVERALL VERDICT: SHIP, with mandatory framing corrections (not SHIP-AS-IS)

The result is **SOUND and clears the ≥95% gate** on every required axis (pooled 0.9951, planner 0.9950, endgame 0.9956, every device cell ≥0.9825, no fallback anywhere). The architecture is genuinely the spec's MoE form (gate + two experts + one shared head emitting the same output neuron in every case), the torch↔JS parity is bit-tight (argmax 1.0000, max|logit| diff 3.9e-8), and the shipped deliverable is faithful to source (verified diff: only local require-path rewrites). The `correctness-no-fallback` lens is right that there is **no hidden fallback** in either regime.

But two of the five lenses (`spec-compliance: flawed`, `generalization-sealed: flawed`) **overstate their own case** and several individual findings are technically wrong. I am downgrading those from "flawed" to "sound-but-must-be-framed-honestly." The result is shippable; the **interpretation as written in HANDOFF.md is not yet adequate and one spec-conformance gap (w_skip) must be disclosed, not hidden.** The single largest risk is that this ships to #5/#6 with the headline "single MoE NN reproduces prod at 99.5%" read as "the NN learned the policy" — which is false and the SPEC itself (§6.123-126) demanded be disclosed.

### Lens claims I am OVERRULING (do not propagate these to the lead):

- **"w_skip≈1 means H contributes only 0.002%/2% of the output" (appears in 4 lenses)** — FALSE. This is a scale-ratio fallacy. `w_skip` multiplies `dec` (a different-magnitude quantity than `H(e)`); a near-1 coefficient on the decisive signal says nothing about H's share of the *argmax-determining* signal. What matters is whether `H(e)` moves the argmax on near-ties — and it demonstrably does (near-tie S_dec rises from the raw-argmax baseline; H learns exactly the marginal-case corrections, as designed). The correct statement is "H learns small corrections," NOT "H is inert / contributes 2%."
- **"endgame per-cell omitted / measurement lacks per-cell breakdown"** (measurement-integrity, major) — FALSE in the code. `moe_eval.py:71-72` and `moe_measure.js:103` both report endgame per-cell. It was abbreviated in the *summary prose*, not missing from the *measurement*. Downgrade to: "disclose the endgame per-cell numbers in HANDOFF, they exist."
- **"feature normalization asymmetry (row[21]=vprior/3 vs row[28]=raw cheapScore) causes numerical instability at distribution shift"** (generalization-sealed, major) — FALSE/incoherent. cheapScore being raw is *required* for the skip term to reproduce prod's argmax on raw scores; normalizing it would break the decisive signal. Both columns are float64 in deploy. No instability mechanism is real here. Drop this finding.
- **"coord-deduping may overstate agreement" (spec-compliance, nit)** — NOT a flaw. SPEC §3 explicitly defines S_dec as coord-deduped; `moe_pack.js` bicls = exact bitwise coord-class. The measurement matches the pinned metric.
- **"w_skip is a spec violation, formula is logit=H(...) not H(...)+w_skip·dec" (spec-compliance, critical)** — PARTIALLY valid but mis-rated. SPEC §3 line 74 shows `logits[slot] = H(g·E_p + (1−g)·E_e)` with no skip term. The implementation adds `+ w_skip·dec`. This IS a literal deviation from the §3 formula. BUT SPEC §4.4 and §3's feature-stage explicitly allow feeding rollout/scan outputs as structure, and §6 anticipates "argmax-of-visible-scores." The skip is a *documented architectural choice*, not leakage. Correct severity: **major (disclosure gap), not critical (not a measurement or leakage violation).** It must be acknowledged in HANDOFF as a deviation from the literal §3 formula, with the §4/§6 justification.

### Lens claims I am UPHOLDING as real:

- **Double-feed of cheapScore (spec-compliance, major) — CONFIRMED.** `E_p` ingests the full 29-dim block including column 28 (cheapScore), and the skip reads column 28 again (`moe_model.py:138`, `moeForward.js:98`). The NN sees the decisive signal on two pathways. SPEC-allowed but architecturally inelegant and must be stated honestly.
- **w_skip not disclosed in the results table (honesty lens, minor) — CONFIRMED.** It's buried in a code comment and only printed at runtime (`moe_eval.py:80`); the HANDOFF results table omits it. Value = 0.9999778800608563 (verified in shipped `moe_weights.json`).
- **SPEC §6.126-required ablation (NN vs raw argmax(cheapScore)) is asserted but not run as a deliverable script (measurement-integrity critical; honesty minor) — CONFIRMED.** HANDOFF.md:52 cites "argmax(cheapScore) agrees with prod 0.99999" and HANDOFF.md:102 lists "Re-run the NN-vs-argmax ablation" as a task *for side-b*. No ablation script is in `repro/`. This is the one SPEC-mandated honesty artifact that is genuinely missing.
- **Zero sealed-seed measurements (generalization-sealed, critical) — CONFIRMED and CORRECT.** All reported numbers are val seeds <270000. `moe_pack.js:25` excludes ≥270000 from packs entirely. Sealed verification is explicitly side-b's job per SPEC §5, so this is not a side-a failure — but the HANDOFF's "Expectation ≥99%" (line 99) is an unverified assertion and must be labeled as such.

## 2. CONSOLIDATED MUST-FIX LIST (ranked by "would mislead the lead/#5/#6 if shipped as-is")

1. **[BLOCKER — honesty] Add an explicit "Honest interpretation" line to the HANDOFF headline.** State plainly: *the NN does NOT learn the policy from kinematics; it learns argmax/argmin of prod's own fed structure outputs (cheapScore = rolled catch+boot or vprior; scan_t). The value-net+rollout that produce cheapScore are load-bearing.* The current HANDOFF §Honesty (lines 50-61) actually says this reasonably well — the fix is to ensure the *results table headline* (line 4) and the #5/#6 post carry the same caveat, so "99.5%" is never quoted bare.

2. **[BLOCKER — spec/disclosure] Disclose `w_skip=0.99998` AND that the deployed logit formula is `H(...) + w_skip·dec`, a documented deviation from SPEC §3 line 74's `logits=H(...)`.** Justify via §4.4/§6 (decisive signal is allowed structure). Put w_skip in the results table. Without this, a reader comparing HANDOFF to SPEC §3 will (correctly) flag undisclosed architecture drift.

3. **[REQUIRED — SPEC §6.126] Ship the ablation script in `repro/` and report its number.** Compute `argmax(cheapScore[col28])` deduped vs prod, and NN-argmax vs `argmax(cheapScore)`, per-regime + per near-tie bucket. SPEC §6 explicitly requires "an ablation (NN vs raw argmax) for honesty." It is currently asserted (0.99999) but not runnable from the deliverable. This is the difference between an honesty *claim* and honesty *evidence*.

4. **[REQUIRED] Quantify H's actual contribution correctly (not as a scale ratio).** Report: on the near-tie subset (dmargin<1e-3), S_dec of (a) raw argmax(cheapScore) vs (b) full MoE forward. This is the *only* honest way to show what H buys, and it directly rebuts the (incorrect) "H is inert" reading the scale-ratio invites. Expectation: full MoE ≥ raw argmax on near-ties, by a measurable margin.

5. **[REQUIRED] Define "endgame 12/12" and surface the endgame/planner per-cell numbers (they exist in the code) in the HANDOFF.** "12/12" is undefined (games? cells? decisions?) and the live n=4713 planner vs n=107564 torch gap needs a one-line explanation (live = different seed sampling, not the same decisions). Small but it reads as cherry-picking if left ambiguous.

6. **[CLEANUP] Fix stale comments in `moeForward.js`** — header line 4 says "...post→LayerNorm" and line 9 says "plannerBlock16x28," both wrong (no LayerNorm per line 63; PLANNER_DIM=29). Code is correct; comments mislead a reviewer auditing parity.

## 3. SINGLE MOST-HONEST ONE-PARAGRAPH FRAMING (for the #5/#6 post)

> **Phase-2 MoE — verdict: PASSES the ≥95% gate; ship with the honest caveat.** A single unified MoE NN (learned gate + planner/endgame experts + one shared output head, no fallback anywhere) reproduces prod's committed decision at **pooled S_dec 0.9951** on held-out val (planner 0.9950 / endgame 0.9956 / every device cell ≥0.9825; live float64 deploy 0.9968; torch↔JS argmax parity 1.0000). The NN is genuinely in the loop for every N≥1 — its argmax IS the decision. **The honest claim is NOT "the NN learned the policy."** By design and per SPEC §3/§4/§6, the NN is *fed prod's own structure outputs* — `cheapScore` (= rolled catch+boot, else vprior) for the planner and `scan_t` for the endgame — so each expert reduces to a learned argmax/argmin over visible decisive scores; ablation shows `argmax(cheapScore)` alone already matches prod 0.99999, the gate is a clean learned step at the N=5/6 boundary, and a learnable skip `w_skip=0.99998` anchors the head to that signal while H learns the marginal-case (near-tie) corrections. The value-net + rollout that *produce* cheapScore are load-bearing; the NN reproduces prod's argmax rule, it does not discover the policy from kinematics. Two deltas from the literal SPEC to note in review: the deployed logit is `H(...) + w_skip·dec` (the §3 formula shows only `H(...)`; the skip is the documented decisive-signal pathway §4 allows), and `cheapScore` is fed both as an expert feature and in the skip (double-pathway, spec-allowed but inelegant). **No sealed-seed numbers yet** — all results are val <270000; sealed verification is side-b's gate.

## 4. RESIDUAL RISK — what side-b MUST independently verify before sign-off

- **Sealed-seed S_dec (≥270000), per-regime + per-cell + per-margin, n≥100/cell.** This is the actual gate and is currently zero-measured. The HANDOFF's "Expectation ≥99%" is an assertion. Drive the shipped `moePolicy.js` (no fallback) through the lockstep harness on sealed seeds. *Note: packs/oracle logs are NOT in the deliverable worktree — side-b must regenerate via `moe_pack.js` from the Phase-1 oracle logs on the VMs; flag if those logs are unavailable, as it blocks reproduction.*
- **The near-tie margin under distribution shift.** Val near-tie (dmargin<1e-3) S_dec is 0.942 — this is the entire residual error budget and the *only* place sealed shift can flip decisions. Verify: (a) raw argmax(cheapScore) really hits ~0.99999 on near-ties (if not, the load-bearing claim is overstated), and (b) the full-MoE near-tie number on sealed doesn't regress below the level that keeps any per-cell ≥0.95. iphone-planner (val 0.9825) is the thinnest cell — confirm it holds on sealed.
- **Independent ablation reproduction (don't trust side-a's 0.99999).** Recompute argmax(cheapScore) vs prod AND NN-argmax vs argmax(cheapScore) yourself; confirm w_skip's effect is "corrects near-ties," not "passes the answer through unchanged."
- **No-fallback audit on the live driver.** Confirm `moe_livetest.js` / your harness never silently falls back to prod argmin when scan_t is unreachable — `moePolicy.js:71-73` returns pure NN argmax even when all scan-t are TMAX-clipped; verify that path on sealed endgame states with all-unreachable boids.

**Files cited (all absolute):** `/workspace/dev/exact_nn/SPEC_PHASE2_MOE.md` (§3 L73-77, §4 L25-27, §6 L113-126); `/workspace/.team/wt-exact-nn-oracle/dev/exact_nn/moe/moe_model.py` (L113-143 skip+double-feed); `.../moe/moe_features.js` (L50,60 raw cheapScore col28); `.../moe/moeForward.js` (L4,9,63 stale comments; L98-99 skip); `.../moe/moePolicy.js` (L71-73 endgame no-fallback); `.../moe/moe_eval.py` (L71-72,80 endgame per-cell + w_skip print); `.../moe/moe_measure.js` (L103 endgame per-cell); `.../moe/moe_pack.js` (L25 sealed exclusion, L91 bicls coord-dedup); `/workspace/.team/exact-nn-moe-student/HANDOFF.md` (L4,52,99,102); `/workspace/.team/exact-nn-moe-student/moe_weights.json` (w_skip=0.9999778800608563, verified).
